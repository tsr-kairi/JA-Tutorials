//numbers
// An integer is a whole number - without a fractional part.
const myNumber = 42;
// A number with a decimal point is a float, with reference the "floating" point.
const myFloat = 42.42;

const myString = "42";
console.log(Number(myString) + myNumber);
